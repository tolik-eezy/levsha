{
  "name": "skill_search",
  "description": "Search the skill registry for available skills matching a query.",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query to find skills by name, description, or tags"
      }
    },
    "required": ["query"]
  },
  "execution": {
    "type": "internal",
    "function": "skill_search"
  }
}
