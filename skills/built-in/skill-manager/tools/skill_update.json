{
  "name": "skill_update",
  "description": "Update an installed skill to the latest version from its repository.",
  "input_schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the skill to update"
      }
    },
    "required": ["name"]
  },
  "execution": {
    "type": "internal",
    "function": "skill_update"
  }
}
