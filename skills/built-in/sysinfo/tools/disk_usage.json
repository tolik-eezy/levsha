{
  "name": "sys_disk",
  "description": "Show disk usage for mounted filesystems. Use when the user asks about disk space, storage, or how full the disk is. Optionally check a specific path.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Optional: specific path to check disk usage for. Omit for all filesystems."
      }
    },
    "required": []
  },
  "execution": {
    "type": "shell",
    "command_template": "df -h {{path | quote}}"
  }
}
