{
  "name": "fs_tail",
  "description": "Show the last N lines of a file. Useful for checking log files or recent entries.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file"
      },
      "lines": {
        "type": "integer",
        "description": "Number of lines to show from the end (default: 20)",
        "default": 20
      }
    },
    "required": ["path"]
  },
  "execution": {
    "type": "shell",
    "command_template": "tail -n {{lines}} {{path | quote}}"
  }
}
