{
  "name": "fs_mkdir",
  "description": "Create a directory. Creates parent directories as needed.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path of the directory to create"
      }
    },
    "required": ["path"]
  },
  "execution": {
    "type": "shell",
    "command_template": "mkdir -p {{path | quote}}"
  }
}
