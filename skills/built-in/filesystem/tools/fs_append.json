{
  "name": "fs_append",
  "description": "Append content to the end of an existing file without replacing its current content.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file to append to"
      },
      "content": {
        "type": "string",
        "description": "Content to append to the file"
      }
    },
    "required": ["path", "content"]
  },
  "execution": {
    "type": "shell",
    "command_template": "echo '{{content}}' >> {{path | quote}}"
  }
}
