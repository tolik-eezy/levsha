{
  "name": "fs_find",
  "description": "Find files by name pattern. Supports shell glob patterns (e.g. '*.log', 'config*').",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Directory to search in (default: current directory)",
        "default": "."
      },
      "pattern": {
        "type": "string",
        "description": "File name pattern to search for (e.g. '*.rs', 'Makefile')"
      },
      "depth": {
        "type": "integer",
        "description": "Maximum search depth (default: 5)",
        "default": 5
      }
    },
    "required": ["pattern"]
  },
  "execution": {
    "type": "shell",
    "command_template": "find {{path | quote}} -name '{{pattern}}' -maxdepth {{depth}}"
  }
}
