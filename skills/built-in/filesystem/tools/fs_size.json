{
  "name": "fs_size",
  "description": "Show the size of a file or directory in human-readable format.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file or directory to check"
      }
    },
    "required": ["path"]
  },
  "execution": {
    "type": "shell",
    "command_template": "du -sh {{path | quote}}"
  }
}
