{
  "name": "fs_read",
  "description": "Read a file's contents. Large files (>30 lines) open in the content panel. Binary files are detected and reported.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file to read"
      }
    },
    "required": ["path"]
  },
  "execution": {
    "type": "internal",
    "function": "fs_read"
  }
}
