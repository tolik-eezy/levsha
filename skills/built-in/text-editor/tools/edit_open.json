{
  "name": "edit_open",
  "description": "Open a file in the text editor buffer for editing. Only one file can be open at a time.",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the file to open"
      }
    },
    "required": ["path"]
  },
  "execution": {
    "type": "internal",
    "function": "edit_open"
  }
}
