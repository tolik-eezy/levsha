{
  "name": "wifi_forget",
  "description": "Remove a saved Wi-Fi network connection profile.",
  "input_schema": {
    "type": "object",
    "properties": {
      "ssid": {
        "type": "string",
        "description": "The SSID (name) of the Wi-Fi network to forget"
      }
    },
    "required": ["ssid"]
  },
  "execution": {
    "type": "shell",
    "command_template": "nmcli connection delete {{ssid | quote()}}"
  }
}
